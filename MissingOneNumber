#include <bits/stdc++.h>

using namespace std;
long long s=0,i,n,t=0,a[1000000];
int main()
{
    cin>>n;
    for(i=1;i<n;i++)
        cin>>a[i];
    for(i=1;i<=n;i++)
    {
         t+=a[i];
        s=s+i;
    }
    cout<<s-t;
    return 0;
}
